require 'spec_helper'

describe <%= config[:app] %>::Controllers::<%= config[:controller] %>::<%= config[:action] %> do
  before do
    @action = <%= config[:app] %>::Controllers::<%= config[:controller] %>::<%= config[:action] %>.new
  end

  it "is successful" do
    response = @action.call({})
    expect(response[0]).to eq 200
  end
end
