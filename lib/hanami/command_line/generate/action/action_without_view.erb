module <%= app.classify %>::Controllers::<%= controller.classify %>
  class <%= action.classify %>
    include <%= app.classify %>::Action

    def call(params)
      self.body = 'OK'
    end
  end
end
